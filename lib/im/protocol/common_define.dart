enum ConversationType { private, group }

enum MessageType { text, image, audio, video, voiceCall, videoCall }

enum MessageDirection { send, receive }

enum MessageStatus { sending, failed, sent, received, read, delete, destroyed }
